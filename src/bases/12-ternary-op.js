const active = true;

// let message = "";

// if (!active) {
//   message = "Active";
// } else {
//   message = "Innactive";
// }

// Regular ternary
// const message = active ? "Active" : "Innactive";

// Shorthand
const message = active && "Active";

console.log(message);
